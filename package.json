{
  "name": "pde-production-root",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "cd server && npm start",
    "heroku-postbuild": "cd server && npm install"
  }
}
